# Orchestration Log: Saul (2026-03-05T20:35:00Z)

**Agent:** Saul (Aspire & Observability)  
**Task:** Fix aspire dotnet launch bug — removed broken dotnet path, made Docker-only  
**Mode:** background  
**Outcome:** SUCCESS  

## What Was Done

1. **Removed broken dotnet path:**
   - Deleted `isDotnetAspireAvailable()`, `commandExists()`, and `launchWithDotnet()` functions from `packages/squad-cli/src/cli/commands/aspire.ts`
   - These paths attempted `dotnet run --project aspire` which fails (Squad has no local aspire project)

2. **Simplified Docker-only approach:**
   - Refactored `isDockerAvailable()` to check `docker --version` directly
   - `runAspire()` now checks Docker availability first — if Docker is missing, errors with install instructions
   - Removed dotnet fallback completely

3. **Fixed port handling:**
   - Updated `launchWithDocker()` to accept `dashboardPort` parameter
   - `--port` flag now correctly propagates to dashboard

4. **Updated help text:**
   - Changed from ".NET Aspire" to "Aspire"
   - Added Docker requirement notice

## Decision Documented

Decision logged to `.squad/decisions/inbox/saul-aspire-docker-only.md`  
Summary: Docker is the universal path. Works for everyone, uses correct Aspire dashboard image, already tested/documented. `--docker` flag still accepted for backward compatibility (no-op).

## Files Modified

- `packages/squad-cli/src/cli/commands/aspire.ts`

## Impact

Anyone running `squad aspire` on a machine with dotnet aspire workload but no Docker now gets clear "install Docker" message instead of cryptic `dotnet run --project aspire` failure.
